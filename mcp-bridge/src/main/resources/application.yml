server:
  port: ${SERVER_PORT:9090}

spring:
  application:
    name: mcp-bridge

mcp:
  bridge:
    relay-url: ${AIRH_RELAY_WS_URL:ws://localhost:8080/ws}
    timeout-seconds: ${AIRH_MCP_TIMEOUT_SECONDS:60}

logging:
  level:
    com.airh.mcp: DEBUG

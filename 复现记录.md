# Phase 09 数据库集成复现记录

## 1. 本次任务目标

读取 `tasks/phase-09-database.md`，为 `relay-server` 接入 PostgreSQL 和 Redis：

- 任务记录写入 PostgreSQL。
- 审计事件写入 PostgreSQL。
- 在线会话状态写入 Redis 缓存，TTL 5 分钟。
- 完成 Maven 全量构建验证。

## 2. 实际完成了什么

- `relay-server/pom.xml` 已加入：
  - `spring-boot-starter-data-jpa`
  - `postgresql`
  - `spring-boot-starter-data-redis`
- `application.yml` 已配置：
  - PostgreSQL：`jdbc:postgresql://localhost:15432/testdb`
  - 用户名：`postgres`
  - 密码：`postgres`
  - Redis：`localhost:16379`
- 新增 JPA 实体：
  - `TaskRecordEntity`
  - `AuditEventEntity`
- 新增 Spring Data Repository：
  - `TaskRecordRepository`
  - `AuditEventRepository`
- `TaskService` 已在创建任务、下发任务、接收任务结果时同步写入/更新数据库。
- 新增 `AuditService`，记录任务创建、下发、结果接收审计事件。
- 新增 `RedisConfig` 和 `SessionStateCache`。
- `DeviceRegistry` 已在 Agent 注册、心跳、断开时刷新 Redis session 缓存。
- 新增 PostgreSQL 建表脚本 `relay-server/src/main/resources/db/migration/V1__init.sql`。
- 新增 Repository CRUD 测试：
  - `TaskRecordRepositoryTest`
  - `AuditEventRepositoryTest`
- 已执行全量构建：`BUILD SUCCESS`。

## 3. 修改/新增了哪些文件

- `relay-server/pom.xml`
- `relay-server/src/main/resources/application.yml`
- `relay-server/src/main/java/com/airh/relay/config/DatabaseConfig.java`
- `relay-server/src/main/java/com/airh/relay/config/RedisConfig.java`
- `relay-server/src/main/java/com/airh/relay/domain/TaskRecordEntity.java`
- `relay-server/src/main/java/com/airh/relay/domain/AuditEventEntity.java`
- `relay-server/src/main/java/com/airh/relay/repository/TaskRecordRepository.java`
- `relay-server/src/main/java/com/airh/relay/repository/AuditEventRepository.java`
- `relay-server/src/main/java/com/airh/relay/service/AuditService.java`
- `relay-server/src/main/java/com/airh/relay/session/SessionStateCache.java`
- `relay-server/src/main/java/com/airh/relay/device/DeviceRegistry.java`
- `relay-server/src/main/java/com/airh/relay/task/TaskRecord.java`
- `relay-server/src/main/java/com/airh/relay/task/TaskService.java`
- `relay-server/src/main/resources/db/migration/V1__init.sql`
- `relay-server/src/test/java/com/airh/relay/repository/TaskRecordRepositoryTest.java`
- `relay-server/src/test/java/com/airh/relay/repository/AuditEventRepositoryTest.java`
- `DEVELOPMENT_REPORT.md`
- `复现记录.md`

## 4. 使用过的关键命令

读取阶段任务：

```powershell
cd E:\openclaw-project\ai-remote-helper
Get-Content -LiteralPath tasks/phase-09-database.md
```

查看 Java 版本：

```powershell
cd E:\openclaw-project\ai-remote-helper
java -version
```

查看 Maven 版本：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -version
```

执行全量构建：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

最终构建结果：

```text
BUILD SUCCESS
common-safety: Tests run: 9, Failures: 0, Errors: 0, Skipped: 0
relay-server: Tests run: 2, Failures: 0, Errors: 0, Skipped: 0
agent-client: Tests run: 17, Failures: 0, Errors: 0, Skipped: 0
全量实际测试合计: Tests run: 28, Failures: 0, Errors: 0, Skipped: 0
```

查看工作区状态：

```powershell
cd E:\openclaw-project\ai-remote-helper
git status --short
```

## 5. 环境要求和版本信息

- 操作系统：Windows 11 10.0 amd64
- Java：OpenJDK 21.0.9 Temurin
- Maven：Apache Maven 3.9.9
- Maven 路径：`E:\openclaw-project\ai-remote-helper\.tools\apache-maven-3.9.9\bin\mvn.cmd`
- Spring Boot：3.3.5
- PostgreSQL：需要监听 `localhost:15432`
- PostgreSQL 数据库：`testdb`
- PostgreSQL 用户名/密码：`postgres/postgres`
- Redis：需要监听 `localhost:16379`

## 6. 如何从零复现

1. 进入项目根目录：

```powershell
cd E:\openclaw-project\ai-remote-helper
```

2. 确认 Java 21 可用：

```powershell
java -version
```

3. 确认项目局部 Maven 可用：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -version
```

4. 准备 PostgreSQL 和 Redis。以下命令本轮未执行，仅建议；如果本机 Docker 可用，可以用它们快速启动依赖：

```powershell
docker run --name airh-postgres -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=testdb -p 15432:5432 -d postgres:16
docker run --name airh-redis -p 16379:6379 -d redis:7
```

5. 检查 `relay-server/src/main/resources/application.yml` 中数据库和 Redis 配置：

```powershell
Get-Content -LiteralPath relay-server/src/main/resources/application.yml
```

6. 执行全量构建：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

7. 如果要手工建表，可执行 `relay-server/src/main/resources/db/migration/V1__init.sql` 中的 SQL。当前项目未引入 Flyway，该 SQL 不会自动执行；运行时默认依赖 Hibernate `ddl-auto=update` 自动建表。

## 7. 启动/运行方式

启动 relay-server：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run
```

如果 8080 端口被占用，可临时换端口：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run -Dspring-boot.run.arguments=--server.port=18081
```

启动 agent-client：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f agent-client\pom.xml javafx:run
```

基本联调流程：

1. 先启动 PostgreSQL 和 Redis。
2. 启动 `relay-server`。
3. 启动 `agent-client`。
4. 在 Agent UI 中手动选择授权目录。
5. 点击连接。
6. 调用 `GET /api/devices/online` 获取 `sessionId`。
7. 调用 `POST /api/sessions/{sessionId}/tasks` 创建任务。
8. 查询 PostgreSQL 中 `task_records` 和 `audit_events` 是否写入记录。
9. 查询 Redis 中 `airh:session:{sessionId}` 是否有在线 session 状态。

## 8. 遇到的问题与解决办法

- 问题：项目之前只有内存任务记录，服务重启后任务状态丢失。
  - 解决：新增 `TaskRecordEntity` 和 `TaskRecordRepository`，在 `TaskService` 中创建、下发、完成任务时写入 PostgreSQL。

- 问题：之前没有统一审计事件入口。
  - 解决：新增 `AuditService` 和 `AuditEventEntity`，记录 `TASK_CREATED`、`TASK_DISPATCHED`、`TASK_RESULT_RECEIVED`。

- 问题：Redis 是缓存依赖，如果 Redis 临时不可用，不应影响内存在线设备状态。
  - 解决：`SessionStateCache` 捕获 Redis 写入/读取异常，保持 Redis 只作为缓存层。

- 问题：`tasks/phase-09-database.md` 要求 `V1__init.sql`，但项目未引入 Flyway。
  - 解决：已新增 SQL 文件用于手工建表或后续迁移工具接入；当前运行时仍依赖 `spring.jpa.hibernate.ddl-auto=update`。

- 问题：本轮未启动 PostgreSQL/Redis 做真实接口联调。
  - 说明：本轮实际执行了 Maven 全量构建、relay-server Repository 测试和既有测试，最终结果 `BUILD SUCCESS`。数据库/Redis 启动命令已在本文标注为“未执行，仅建议”。

- 问题：补充 Repository 测试后第一次全量构建失败。
  - 原因：relay-server 测试已通过，但 Windows 文件锁导致 Maven clean 删除 `agent-client\target\classes\com\airh` 失败。
  - 解决：等待文件锁释放后重跑同一条命令，最终 `BUILD SUCCESS`。

## 9. 后续注意事项

- 生产环境不要长期使用 `ddl-auto=update`，建议后续引入 Flyway 或 Liquibase 管理迁移。
- `application.yml` 中当前是本地开发数据库凭据，不要直接用于公开环境。
- 当前只持久化任务记录和审计事件，任务日志 `TaskLog` 仍在内存中。
- 如需验证数据库写入，必须先保证 PostgreSQL 和 Redis 分别监听 `15432`、`16379`。
- 后续可以补充基于 Testcontainers 的 Repository 集成测试，避免依赖开发机已有数据库状态。

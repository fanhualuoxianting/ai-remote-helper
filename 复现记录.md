# Phase 02 复现记录：Agent 与 Relay Server 基础连接

## 1. 本次任务目标

只完成 `tasks/phase-02-agent-server-connection.md` 指定的 Phase 02：实现 `agent-client` 主动连接 `relay-server` 的基础 WebSocket/STOMP 连接闭环。

本阶段范围：

- Agent 输入服务器地址并连接 Relay Server。
- Agent 发送 hello。
- Relay Server 注册在线设备，生成 `sessionId` 和 6 位连接码。
- Agent UI 显示已连接、`deviceId`、连接码、授权目录和实时日志。
- Agent 定时发送 heartbeat。
- Agent 断开后停止 heartbeat。
- Relay Server 收到 WebSocket 断开事件后标记设备离线。
- 提供 `GET /api/health` 和 `GET /api/devices/online`。

明确不做：

- 不实现远程命令执行。
- 不实现文件读写。
- 不实现 MCP 工具。
- 不实现 Phase 03 的任务转发。
- 不接数据库，设备状态只使用内存存储。

## 2. 实际完成了什么

- `common-protocol` 新增 Phase 02 所需基础消息 DTO。
- `relay-server` 新增 WebSocket/STOMP 配置、Agent hello/heartbeat 消息处理、内存在线设备注册表、断开事件监听和在线设备 REST 查询。
- `agent-client` JavaFX UI 从占位窗口增强为可输入服务器地址、选择授权目录、连接、断开并显示连接码的基础客户端。
- 保留安全边界：连接前必须手动选择授权目录；Agent UI 可见；没有任何命令执行、文件读写、任务转发或 MCP 工具实现。
- 执行 Maven 构建检查，所有模块编译通过。
- 执行 relay-server 最小启动验证，`/api/health` 和 `/api/devices/online` 可访问。

## 3. 修改/新增了哪些文件

```text
common-protocol/src/main/java/com/airh/protocol/enums/MessageType.java
common-protocol/src/main/java/com/airh/protocol/dto/AgentHelloMessage.java
common-protocol/src/main/java/com/airh/protocol/dto/AgentOnlineMessage.java
common-protocol/src/main/java/com/airh/protocol/dto/HeartbeatMessage.java
common-protocol/src/main/java/com/airh/protocol/dto/ErrorMessage.java
relay-server/src/main/java/com/airh/relay/config/SecurityConfig.java
relay-server/src/main/java/com/airh/relay/config/WebSocketConfig.java
relay-server/src/main/java/com/airh/relay/controller/HealthController.java
relay-server/src/main/java/com/airh/relay/controller/DeviceController.java
relay-server/src/main/java/com/airh/relay/device/DeviceConnection.java
relay-server/src/main/java/com/airh/relay/device/DeviceRegistry.java
relay-server/src/main/java/com/airh/relay/websocket/AgentConnectionController.java
relay-server/src/main/java/com/airh/relay/websocket/AgentWebSocketEventListener.java
agent-client/pom.xml
agent-client/src/main/java/com/airh/agent/connection/AgentConnectionClient.java
agent-client/src/main/java/com/airh/agent/connection/AgentConnectionListener.java
agent-client/src/main/java/com/airh/agent/ui/AgentClientApplication.java
DEVELOPMENT_REPORT.md
复现记录.md
```

## 4. 使用过的关键命令

检查 Maven 是否在 PATH 中：

```powershell
where.exe mvn
mvn -version
```

实际结果：未找到系统 Maven。

检查 Java 版本：

```powershell
java -version
```

实际结果：OpenJDK 21.0.9。

下载并使用项目局部 Maven 3.9.9：

```powershell
cd E:\openclaw-project\ai-remote-helper
New-Item -ItemType Directory -Force -Path .tools | Out-Null
$zip = Join-Path (Resolve-Path .tools) 'apache-maven-3.9.9-bin.zip'
Invoke-WebRequest -Uri 'https://archive.apache.org/dist/maven/maven-3/3.9.9/binaries/apache-maven-3.9.9-bin.zip' -OutFile $zip
Expand-Archive -Path $zip -DestinationPath .tools -Force
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -version
```

执行完整构建检查：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

为单模块 `spring-boot:run` 验证安装父 POM 和 common 模块到本地 Maven 仓库：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd install -N
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -pl common-protocol,common-safety install
```

启动 relay-server 做最小验证：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run -Dspring-boot.run.arguments=--server.port=18081
```

访问 API：

```powershell
Invoke-RestMethod http://localhost:18081/api/health
Invoke-RestMethod http://localhost:18081/api/devices/online
```

实际验证结果：

```text
GET /api/health -> {"status":"UP"}
GET /api/devices/online -> []
```

启动 Agent GUI 的命令，本轮未执行，仅建议：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f agent-client\pom.xml javafx:run
```

## 5. 环境要求和版本信息

- 操作系统：Windows 11
- Shell：PowerShell
- Java：OpenJDK 21.0.9
- Maven：本机 PATH 未配置 Maven；本轮下载并使用项目局部 Apache Maven 3.9.9
- Spring Boot：3.3.5
- JavaFX：21.0.5
- WebSocket/STOMP：Spring WebSocket + Spring Messaging
- Agent STOMP 客户端运行时：Tyrus standalone client 2.1.5

## 6. 如何从零复现

1. 进入项目目录：

```powershell
cd E:\openclaw-project\ai-remote-helper
```

2. 确认 Java 21 可用：

```powershell
java -version
```

3. 如果系统已有 Maven，直接检查：

```powershell
mvn -version
```

4. 如果系统没有 Maven，下载项目局部 Maven：

```powershell
New-Item -ItemType Directory -Force -Path .tools | Out-Null
$zip = Join-Path (Resolve-Path .tools) 'apache-maven-3.9.9-bin.zip'
Invoke-WebRequest -Uri 'https://archive.apache.org/dist/maven/maven-3/3.9.9/binaries/apache-maven-3.9.9-bin.zip' -OutFile $zip
Expand-Archive -Path $zip -DestinationPath .tools -Force
```

5. 编译整个项目：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

如果使用系统 Maven，则执行：

```powershell
mvn clean package
```

6. 单独启动 relay-server 前，先安装父 POM 和 common 模块：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd install -N
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -pl common-protocol,common-safety install
```

7. 启动 relay-server：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run
```

如果 8080 被占用，换端口：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run -Dspring-boot.run.arguments=--server.port=18081
```

8. 打开另一个 PowerShell，验证 relay-server：

```powershell
Invoke-RestMethod http://localhost:8080/api/health
Invoke-RestMethod http://localhost:8080/api/devices/online
```

如果使用 18081 端口：

```powershell
Invoke-RestMethod http://localhost:18081/api/health
Invoke-RestMethod http://localhost:18081/api/devices/online
```

9. 启动 Agent GUI：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f agent-client\pom.xml javafx:run
```

10. 在 Agent GUI 中操作：

- 服务器地址填写 `http://localhost:8080`，或临时端口 `http://localhost:18081`。
- 点击“选择授权目录”，手动选择一个工作目录。
- 点击“连接”。
- 看到“当前状态：已连接”和 6 位连接码。
- 调用 `GET /api/devices/online`，应能看到在线设备。
- 点击“断开”，再调用 `GET /api/devices/online`，设备应不再在线。

## 7. 启动/运行方式

启动 Relay Server：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run
```

启动 Agent Client：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f agent-client\pom.xml javafx:run
```

健康检查：

```powershell
Invoke-RestMethod http://localhost:8080/api/health
```

在线设备查询：

```powershell
Invoke-RestMethod http://localhost:8080/api/devices/online
```

## 8. 遇到的问题与解决办法

问题 1：系统 PATH 中没有 Maven。

解决办法：下载 Apache Maven 3.9.9 到项目 `.tools` 目录，并使用 `.\.tools\apache-maven-3.9.9\bin\mvn.cmd` 执行构建。

问题 2：第一次启动验证使用 `-pl relay-server spring-boot:run`，Maven 没有解析本地 common 模块。

解决办法：改为先执行：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd install -N
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -pl common-protocol,common-safety install
```

然后执行：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run
```

问题 3：STOMP 心跳 scheduler bean 与 Spring 内置 bean 名称冲突。

解决办法：将自定义 bean 改名为 `stompHeartbeatTaskScheduler`。

问题 4：本机 8080 端口已被 PID 8116 占用。

解决办法：不关闭现有进程，使用临时端口 18081 验证：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run -Dspring-boot.run.arguments=--server.port=18081
```

问题 5：用 `Start-Process` 停止 Maven wrapper 进程时，子 Java 进程可能继续占用端口。

解决办法：验证后根据端口查询 PID 并停止对应验证进程。不要停止 8080 上的既有进程。

## 9. 后续注意事项

- Phase 02 只使用内存存储，服务重启后在线设备状态会丢失，这是本阶段预期行为。
- 后续接 PostgreSQL/Redis 时，应替换 `DeviceRegistry` 的内部存储实现，不要把数据库逻辑散落到 WebSocket Controller。
- 进入 Phase 03 前，不要在 Agent 端添加命令执行、文件读写或任务处理逻辑。
- Agent 连接前必须继续要求用户手动选择授权目录。
- 断开连接后必须停止 heartbeat，后续也不得继续接收任务。
- 后续如果要做完整端到端验证，需要人工启动 JavaFX Agent 并点击连接；本轮只做了 Maven 编译和 relay-server REST 最小启动验证。

# 安全说明

AI Remote Helper 的核心原则是明确授权、可见操作、目录边界和审计留痕。

## 安全边界

- Agent 客户端必须始终可见，不能隐藏运行。
- 用户必须手动选择授权目录。
- 文件操作只能发生在授权目录内。
- 命令执行的工作目录必须限制在授权目录内。
- 每个会话需要有有效期。
- 所有远程操作需要记录审计日志。
- 文件修改前需要备份。
- 高风险命令需要被识别并拦截。

## 禁止能力

本项目禁止实现以下能力：

- 隐藏运行或伪装进程。
- 绕过用户授权。
- 默认开机自启动。
- 后门持久化。
- 提权。
- 反调试、反杀进程。
- 关闭防火墙或杀毒软件。
- 读取浏览器数据、SSH 私钥、系统凭证。
- 访问授权目录外文件。
- 删除系统目录、格式化磁盘。
- 修改注册表、添加系统用户。
- 规避审计日志。

## 授权目录机制规划

后续阶段中，Agent 客户端会要求用户手动选择一个工作目录。所有文件读取、文件写入、目录列表和命令执行都会经过路径校验，确保目标路径在授权目录内。

Phase 01 仅创建 `PathGuard` 框架和基础路径判断方法，不实现完整任务执行链路。

## 审计机制规划

后续阶段中，relay-server 和 agent-client 都需要记录任务请求、用户授权、任务执行状态、输出摘要、失败原因和断开事件。审计日志不能被远程任务绕过或关闭。

Phase 01 仅预留 audit 包和协议 DTO，不实现日志持久化。

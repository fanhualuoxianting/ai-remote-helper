# Phase 01 复现记录

## 1. 本次任务目标

严格阅读 `AGENTS.md` 和 `tasks/phase-01-project-skeleton.md`，只完成 Phase 01：创建 Java 版 Maven 多模块项目骨架。不得实现后续阶段，不实现真实远程命令执行。完成后运行必要构建检查，并更新 `DEVELOPMENT_REPORT.md`。

## 2. 实际完成了什么

已创建 AI Remote Helper 的 Java 版项目骨架：

- 根目录父级 Maven `pom.xml`。
- `common-protocol`、`common-safety`、`relay-server`、`agent-client`、`mcp-bridge` 五个 Maven 子模块。
- `web-console`、`docs`、`tasks` 目录。
- 协议枚举和基础 DTO。
- 安全规则框架类。
- Spring Boot 空服务。
- JavaFX 基础可见窗口。
- MCP Bridge 占位模块。
- 中文 README、安全说明、部署说明、架构说明、路线图和开发报告。

本阶段没有实现：

- 真实远程命令执行。
- 文件读取或写入任务执行链路。
- Agent 与 relay-server 的真实连接。
- MCP 工具注册和调用。
- 隐藏运行、开机自启、提权、后门持久化等禁止能力。

## 3. 修改/新增了哪些文件

新增或修改的主要文件如下：

```text
pom.xml
README.md
SECURITY.md
DEPLOY.md
DEVELOPMENT_REPORT.md
复现记录.md
common-protocol/pom.xml
common-protocol/src/main/java/com/airh/protocol/enums/MessageType.java
common-protocol/src/main/java/com/airh/protocol/enums/TaskType.java
common-protocol/src/main/java/com/airh/protocol/enums/TaskStatus.java
common-protocol/src/main/java/com/airh/protocol/enums/PermissionType.java
common-protocol/src/main/java/com/airh/protocol/enums/RiskLevel.java
common-protocol/src/main/java/com/airh/protocol/dto/RemoteMessage.java
common-protocol/src/main/java/com/airh/protocol/dto/RemoteTask.java
common-protocol/src/main/java/com/airh/protocol/dto/TaskResult.java
common-protocol/src/main/java/com/airh/protocol/dto/TaskLog.java
common-safety/pom.xml
common-safety/src/main/java/com/airh/safety/CommandRiskDetector.java
common-safety/src/main/java/com/airh/safety/PathGuard.java
common-safety/src/main/java/com/airh/safety/SensitiveFileGuard.java
relay-server/pom.xml
relay-server/src/main/java/com/airh/relay/RelayServerApplication.java
relay-server/src/main/java/com/airh/relay/config/SecurityConfig.java
relay-server/src/main/java/com/airh/relay/controller/HealthController.java
relay-server/src/main/resources/application.yml
agent-client/pom.xml
agent-client/src/main/java/com/airh/agent/ui/AgentClientApplication.java
mcp-bridge/pom.xml
mcp-bridge/src/main/java/com/airh/mcp/McpBridgeApplication.java
web-console/README.md
docs/ARCHITECTURE.md
docs/ROADMAP.md
```

另外，各模块中还新增了若干 `package-info.java`，用于预留 Phase 01 要求的 package 分层。

## 4. 使用过的关键命令

读取任务文件：

```powershell
Get-Content -Raw -LiteralPath AGENTS.md
Get-Content -Raw -LiteralPath tasks\phase-01-project-skeleton.md
```

查看仓库状态和文件：

```powershell
git status --short
Get-ChildItem -Force
Get-ChildItem -Recurse -Force -File | Select-Object -ExpandProperty FullName
```

检查 Java 和 Maven：

```powershell
java -version
where.exe mvn
where.exe java
```

查找 Maven 和做兜底检查：

```powershell
Get-ChildItem -Path C:\,D:\,E:\ -Recurse -Filter mvn.cmd -ErrorAction SilentlyContinue | Select-Object -First 10 -ExpandProperty FullName
javac -version
Get-ChildItem -Recurse -Filter pom.xml | ForEach-Object { [xml](Get-Content -Raw -LiteralPath $_.FullName) | Out-Null; $_.FullName }
```

公共模块源码编译检查：

```powershell
$files = Get-ChildItem -Recurse -Filter *.java common-protocol,common-safety,mcp-bridge | Select-Object -ExpandProperty FullName
New-Item -ItemType Directory -Force -Path .verify\classes | Out-Null
javac -encoding UTF-8 -d .verify\classes $files
```

创建目录：

```powershell
New-Item -ItemType Directory -Force -Path common-protocol\src\main\java\com\airh\protocol\dto,common-protocol\src\main\java\com\airh\protocol\enums,common-safety\src\main\java\com\airh\safety,relay-server\src\main\java\com\airh\relay\config,relay-server\src\main\java\com\airh\relay\controller,relay-server\src\main\java\com\airh\relay\service,relay-server\src\main\java\com\airh\relay\repository,relay-server\src\main\java\com\airh\relay\domain,relay-server\src\main\java\com\airh\relay\dto,relay-server\src\main\java\com\airh\relay\websocket,relay-server\src\main\java\com\airh\relay\session,relay-server\src\main\java\com\airh\relay\device,relay-server\src\main\java\com\airh\relay\task,relay-server\src\main\java\com\airh\relay\audit,relay-server\src\main\resources,agent-client\src\main\java\com\airh\agent\ui,agent-client\src\main\java\com\airh\agent\connection,agent-client\src\main\java\com\airh\agent\executor,agent-client\src\main\java\com\airh\agent\filesystem,agent-client\src\main\java\com\airh\agent\safety,agent-client\src\main\java\com\airh\agent\audit,agent-client\src\main\java\com\airh\agent\report,mcp-bridge\src\main\java\com\airh\mcp\tools,mcp-bridge\src\main\java\com\airh\mcp\client,mcp-bridge\src\main\java\com\airh\mcp\config,web-console,docs,tasks | Out-Null
```

构建检查命令：

```powershell
mvn clean package
```

说明：该命令已实际尝试执行，但需要 Maven 可用。当前环境中 `mvn` 不在 PATH 中，因此命令失败。

失败输出摘要：

```text
mvn: The term 'mvn' is not recognized as a name of a cmdlet, function, script file, or executable program.
```

## 5. 环境要求和版本信息

要求：

- Java 21
- Maven 3.9 或兼容版本
- Windows PowerShell

本机已确认：

```text
openjdk version "21.0.9" 2025-10-21 LTS
OpenJDK Runtime Environment Temurin-21.0.9+10
OpenJDK 64-Bit Server VM Temurin-21.0.9+10
```

已完成的兜底检查：

```text
javac 21.0.9
common-protocol、common-safety、mcp-bridge 源码 javac 编译通过
全部 pom.xml XML 解析通过
```

本机当前问题：

```text
where.exe mvn
INFO: Could not find files for the given pattern(s).
```

## 6. 如何从零复现

1. 准备 Java 21 和 Maven。

```powershell
java -version
mvn -v
```

2. 进入项目根目录。

```powershell
cd E:\openclaw-project\ai-remote-helper
```

3. 检查 Maven 多模块结构。

```powershell
Get-ChildItem -Force
Get-ChildItem common-protocol,common-safety,relay-server,agent-client,mcp-bridge
```

4. 构建全部模块。

```powershell
mvn clean package
```

5. 如果 Maven 不可用，先安装 Maven 并把 `mvn.cmd` 所在目录加入 PATH，然后重新打开 PowerShell 再执行：

```powershell
mvn -v
mvn clean package
```

## 7. 启动/运行方式

启动 relay-server：

```powershell
mvn -pl relay-server spring-boot:run
```

启动后访问健康检查：

```powershell
Invoke-RestMethod http://localhost:8080/health
```

启动 agent-client：

```powershell
mvn -pl agent-client javafx:run
```

说明：上述启动命令需要 Maven 可用。当前环境中 Maven 未在 PATH 中，因此这些命令本轮未执行，仅建议。

## 8. 遇到的问题与解决办法

问题：当前 PowerShell 中 `mvn` 不可用。

现象：

```text
mvn: The term 'mvn' is not recognized as a name of a cmdlet, function, script file, or executable program.
where.exe mvn
INFO: Could not find files for the given pattern(s).
```

解决办法：

1. 安装 Maven 3.9 或兼容版本。
2. 将 Maven 的 `bin` 目录加入用户或系统 PATH。
3. 重新打开 PowerShell。
4. 执行：

```powershell
mvn -v
mvn clean package
```

## 9. 后续注意事项

- 后续必须按 `tasks/phase-02-agent-server-connection.md` 继续，不要跨阶段实现。
- 任何文件操作都必须限制在用户授权目录内。
- 任何命令执行都必须限制 cwd 在授权目录内。
- Agent 客户端必须可见，并提供断开能力。
- 所有阶段完成后都要更新 `DEVELOPMENT_REPORT.md` 和本文件。

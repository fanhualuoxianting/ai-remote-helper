# Phase 03 复现记录：任务转发协议与模拟结果

## 1. 本次任务目标

只完成 `tasks/phase-03-task-routing.md` 指定的 Phase 03：实现任务转发协议闭环，不执行真实命令，不读取真实文件，不写入真实文件，不做后续阶段。

本阶段目标：

- Controller/MCP 侧通过 relay-server 创建任务。
- relay-server 根据 `sessionId` 找到在线 Agent，并下发任务。
- agent-client 接收任务后必须在 UI 中显示任务信息。
- agent-client 返回模拟结果。
- relay-server 支持查询任务结果和任务日志。

明确不做：

- 不执行真实命令。
- 不读取真实文件。
- 不写入真实文件。
- 不应用真实补丁。
- 不实现数据库持久化。
- 不实现后续阶段的真实执行器。

## 2. 实际完成了什么

- `common-protocol` 补充 `CreateTaskRequest`、`CreateTaskResponse`、`TaskPayload`、`TaskLogMessage`、`TaskResultMessage`。
- `common-protocol` 调整任务类型和任务状态，支持 Phase 03 验收要求中的 `LIST_DIR`、`READ_FILE`、`RUN_COMMAND`、`WRITE_FILE`、`APPLY_PATCH` 和 `PENDING`、`RUNNING`、`SUCCESS`、`FAILED`、`TIMEOUT`、`CANCELLED`、`BLOCKED`。
- `relay-server` 新增任务创建、任务查询、任务日志查询 API。
- `relay-server` 用内存保存任务状态和任务日志。
- `relay-server` 通过 STOMP 将任务下发到对应 Agent topic。
- `relay-server` 接收 Agent 上报的 `TASK_LOG` 和 `TASK_RESULT`。
- `agent-client` 收到任务后在 UI 日志区显示 `taskId`、`taskType`、payload 摘要、任务开始、任务日志、任务结束。
- `agent-client` 对所有本阶段任务类型返回模拟结果，没有任何真实命令执行或真实文件读写。
- 已使用项目局部 Maven 完成父 POM install、common 模块 install 和全量构建检查。

## 3. 修改/新增了哪些文件

```text
common-protocol/src/main/java/com/airh/protocol/enums/TaskType.java
common-protocol/src/main/java/com/airh/protocol/enums/TaskStatus.java
common-protocol/src/main/java/com/airh/protocol/dto/RemoteTask.java
common-protocol/src/main/java/com/airh/protocol/dto/TaskResult.java
common-protocol/src/main/java/com/airh/protocol/dto/CreateTaskRequest.java
common-protocol/src/main/java/com/airh/protocol/dto/CreateTaskResponse.java
common-protocol/src/main/java/com/airh/protocol/dto/TaskPayload.java
common-protocol/src/main/java/com/airh/protocol/dto/TaskLogMessage.java
common-protocol/src/main/java/com/airh/protocol/dto/TaskResultMessage.java
relay-server/src/main/java/com/airh/relay/config/SecurityConfig.java
relay-server/src/main/java/com/airh/relay/controller/TaskController.java
relay-server/src/main/java/com/airh/relay/device/DeviceRegistry.java
relay-server/src/main/java/com/airh/relay/task/TaskRecord.java
relay-server/src/main/java/com/airh/relay/task/TaskService.java
relay-server/src/main/java/com/airh/relay/websocket/AgentConnectionController.java
agent-client/src/main/java/com/airh/agent/connection/AgentConnectionClient.java
agent-client/src/main/java/com/airh/agent/connection/AgentConnectionListener.java
agent-client/src/main/java/com/airh/agent/ui/AgentClientApplication.java
DEVELOPMENT_REPORT.md
复现记录.md
```

## 4. 使用过的关键命令

进入项目目录：

```powershell
cd E:\openclaw-project\ai-remote-helper
```

安装父 POM：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd install -N
```

安装 common 模块：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -pl common-protocol,common-safety install
```

构建全部模块：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

查看工作区变更：

```powershell
git status --short
git diff --stat
```

启动 relay-server 的命令，本轮未执行，仅建议：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run
```

启动 agent-client GUI 的命令，本轮未执行，仅建议：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f agent-client\pom.xml javafx:run
```

创建任务 API 调用命令，本轮未执行，仅建议：

```powershell
$body = @{
  taskType = "LIST_DIR"
  payload = @{
    data = @{
      path = "."
    }
  }
  timeoutSeconds = 30
} | ConvertTo-Json -Depth 5

Invoke-RestMethod -Method Post -Uri "http://localhost:8080/api/sessions/<sessionId>/tasks" -ContentType "application/json" -Body $body
```

查询任务结果命令，本轮未执行，仅建议：

```powershell
Invoke-RestMethod "http://localhost:8080/api/tasks/<taskId>"
```

查询任务日志命令，本轮未执行，仅建议：

```powershell
Invoke-RestMethod "http://localhost:8080/api/tasks/<taskId>/logs"
```

## 5. 环境要求和版本信息

- 操作系统：Windows
- Shell：PowerShell
- Java：项目要求 Java 21
- Maven：必须使用项目局部 Maven `.tools/apache-maven-3.9.9/bin/mvn.cmd`
- Spring Boot：3.3.5
- JavaFX：21.0.5
- 通信方式：Spring WebSocket / STOMP
- 当前存储：内存存储任务、日志和在线设备状态

## 6. 如何从零复现

1. 进入项目目录：

```powershell
cd E:\openclaw-project\ai-remote-helper
```

2. 确认 Java 21 可用：

```powershell
java -version
```

3. 确认项目局部 Maven 存在：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -version
```

4. 安装父 POM：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd install -N
```

5. 安装 common 模块：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -pl common-protocol,common-safety install
```

6. 构建全部模块：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

7. 启动 relay-server：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run
```

如果 8080 端口被占用，使用临时端口：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run -Dspring-boot.run.arguments=--server.port=18081
```

8. 打开另一个 PowerShell，启动 Agent GUI：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f agent-client\pom.xml javafx:run
```

9. 在 Agent GUI 中操作：

- 服务器地址填写 `http://localhost:8080`，如果 relay-server 使用 18081，则填写 `http://localhost:18081`。
- 点击“选择授权目录”，手动选择工作目录。
- 点击“连接”。
- 看到“当前状态：已连接”和 6 位连接码。

10. 查询在线设备，拿到 `sessionId`：

```powershell
Invoke-RestMethod http://localhost:8080/api/devices/online
```

如果使用 18081：

```powershell
Invoke-RestMethod http://localhost:18081/api/devices/online
```

11. 创建一个模拟 `LIST_DIR` 任务：

```powershell
$body = @{
  taskType = "LIST_DIR"
  payload = @{
    data = @{
      path = "."
    }
  }
  timeoutSeconds = 30
} | ConvertTo-Json -Depth 5

Invoke-RestMethod -Method Post -Uri "http://localhost:8080/api/sessions/<sessionId>/tasks" -ContentType "application/json" -Body $body
```

12. 到 Agent UI 查看日志，应能看到收到任务、payload 摘要、模拟处理和任务结束。

13. 查询任务结果：

```powershell
Invoke-RestMethod "http://localhost:8080/api/tasks/<taskId>"
```

14. 查询任务日志：

```powershell
Invoke-RestMethod "http://localhost:8080/api/tasks/<taskId>/logs"
```

## 7. 启动/运行方式

启动 relay-server：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run
```

启动 agent-client：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f agent-client\pom.xml javafx:run
```

查询在线 Agent：

```powershell
Invoke-RestMethod http://localhost:8080/api/devices/online
```

创建任务：

```powershell
$body = @{
  taskType = "RUN_COMMAND"
  payload = @{
    data = @{
      command = "mvn test"
    }
  }
  timeoutSeconds = 30
} | ConvertTo-Json -Depth 5

Invoke-RestMethod -Method Post -Uri "http://localhost:8080/api/sessions/<sessionId>/tasks" -ContentType "application/json" -Body $body
```

注意：上面的 `RUN_COMMAND` 只会返回模拟 stdout/stderr，不会执行 `mvn test`。

## 8. 遇到的问题与解决办法

问题 1：必须先 install 父 POM 和 common 模块。

解决办法：按要求先执行：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd install -N
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -pl common-protocol,common-safety install
```

然后再执行：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

问题 2：本阶段任务类型与旧枚举不完全一致。

解决办法：将 `TaskType` 对齐为 Phase 03 需要的 `LIST_DIR`、`READ_FILE`、`RUN_COMMAND`、`WRITE_FILE`、`APPLY_PATCH`，保留 `GENERATE_REPORT` 作为已有类型。

问题 3：本阶段不能执行真实命令或读写文件。

解决办法：Agent 端只在 `AgentConnectionClient` 中根据 `taskType` 构造模拟结果，并通过 `/app/agent/task-result` 返回；没有调用 `ProcessBuilder`，没有读取磁盘文件，没有写入授权目录或其它目录。

问题 4：本轮没有自动打开 JavaFX GUI。

解决办法：本轮只执行 Maven 构建检查；GUI 端到端人工验证命令已在本文档中标注为“未执行，仅建议”。

## 9. 后续注意事项

- Phase 03 仍然是协议闭环阶段，不要把模拟执行误认为真实执行。
- 任务和日志现在保存在内存中，服务重启后会丢失。
- `timeoutSeconds` 已进入协议和任务记录，但当前没有后台扫描器自动置为 `TIMEOUT`。
- 后续阶段如果实现真实执行，必须继续限制 cwd 在授权目录内，并加入危险命令拦截、敏感文件保护、任务超时、修改前备份和审计日志。
- Agent UI 必须始终可见，所有任务必须显示在 UI 日志区。
- 不得实现隐藏运行、绕过授权、访问授权目录外文件、读取浏览器数据、读取 SSH 私钥、提权、修改注册表或规避审计日志。

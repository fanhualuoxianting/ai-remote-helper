# Phase 04 复现记录：授权目录选择与路径沙箱

## 1. 本次任务目标

根据 `tasks/phase-04-path-sandbox.md` 完成 Phase 04：Agent 启动后必须先由用户手动选择授权目录，后续路径都必须限制在授权目录内；授权目录需要通过连接协议传递给 Relay Server，并在任务下发时携带给 Agent。

本阶段不实现真实文件读取、真实文件写入、真实命令执行或后续阶段能力。

## 2. 实际完成了什么

- 新增 `AuthorizedDirectoryChooser`，封装 JavaFX 目录选择器。
- 新增 `PathSandbox`，支持授权目录内路径校验、安全解析相对路径和路径标准化。
- 更新 `AgentClientApplication`，选择目录后创建路径沙箱，未选择授权目录时禁止连接。
- `AgentConnectionClient` 连接时继续发送 `authorizedDirectory`，收到任务时记录服务端下发的授权目录。
- Relay Server 在 hello 阶段校验 `authorizedDirectory`，缺失时拒绝注册。
- `DeviceRegistry` 记录授权目录，并新增 `getAuthorizedDirectory(deviceId)`。
- `RemoteTask` 增加 `authorizedDirectory` 字段，`TaskService` 下发任务时携带授权目录。
- 新增 `PathSandboxTest`，验证正常相对路径允许、路径穿越拒绝。
- 已执行全量 Maven 构建检查，实际结果为 `BUILD SUCCESS`。

## 3. 修改/新增了哪些文件

```text
agent-client/pom.xml
agent-client/src/main/java/com/airh/agent/safety/PathSandbox.java
agent-client/src/main/java/com/airh/agent/ui/AuthorizedDirectoryChooser.java
agent-client/src/main/java/com/airh/agent/ui/AgentClientApplication.java
agent-client/src/main/java/com/airh/agent/connection/AgentConnectionClient.java
agent-client/src/test/java/com/airh/agent/safety/PathSandboxTest.java
common-protocol/src/main/java/com/airh/protocol/dto/RemoteTask.java
relay-server/src/main/java/com/airh/relay/device/DeviceRegistry.java
relay-server/src/main/java/com/airh/relay/task/TaskService.java
relay-server/src/main/java/com/airh/relay/websocket/AgentConnectionController.java
DEVELOPMENT_REPORT.md
复现记录.md
```

## 4. 使用过的关键命令

进入项目目录：

```powershell
cd E:\openclaw-project\ai-remote-helper
```

按要求执行 Maven 构建：

```powershell
mvn clean package
```

实际结果：失败。当前 PowerShell PATH 中没有 `mvn`，报错为 `The term 'mvn' is not recognized`。

使用项目局部 Maven 执行同等构建：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

实际结果：成功，Maven 输出 `BUILD SUCCESS`。

查看 Java 版本：

```powershell
java -version
```

查看项目局部 Maven 版本：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -version
```

查看变更状态：

```powershell
git status --short
git diff --stat
```

启动 relay-server，本轮未执行，仅建议：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run
```

启动 agent-client GUI，本轮未执行，仅建议：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f agent-client\pom.xml javafx:run
```

查询在线设备，本轮未执行，仅建议：

```powershell
Invoke-RestMethod http://localhost:8080/api/devices/online
```

创建模拟任务，本轮未执行，仅建议：

```powershell
$body = @{
  taskType = "LIST_DIR"
  payload = @{
    data = @{
      path = "src/main.java"
    }
  }
  timeoutSeconds = 30
} | ConvertTo-Json -Depth 5

Invoke-RestMethod -Method Post -Uri "http://localhost:8080/api/sessions/<sessionId>/tasks" -ContentType "application/json" -Body $body
```

## 5. 环境要求和版本信息

- 操作系统：Windows 11
- Shell：PowerShell
- Java：OpenJDK 21.0.9 Temurin
- Maven：项目局部 Apache Maven 3.9.9
- Spring Boot：3.3.5
- JavaFX：21.0.5
- JUnit Jupiter：5.10.5
- Maven Surefire Plugin：3.2.5

## 6. 如何从零复现

1. 进入项目目录：

```powershell
cd E:\openclaw-project\ai-remote-helper
```

2. 确认 Java 21 可用：

```powershell
java -version
```

3. 确认项目局部 Maven 可用：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -version
```

4. 执行全量构建和测试：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

5. 确认测试结果中包含：

```text
Running com.airh.agent.safety.PathSandboxTest
Tests run: 2, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

6. 启动 relay-server：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run
```

如果 8080 端口被占用，使用临时端口：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run -Dspring-boot.run.arguments=--server.port=18081
```

7. 另开一个 PowerShell，启动 Agent GUI：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f agent-client\pom.xml javafx:run
```

8. 在 Agent GUI 中操作：

- 服务器地址填写 `http://localhost:8080`；如果 Relay 使用 18081，则填写 `http://localhost:18081`。
- 先点击“选择授权目录”，手动选择一个工作目录。
- 再点击“连接”。
- 如果未选择授权目录直接点击“连接”，UI 应提示“连接失败：请先手动选择授权目录”。
- 连接成功后 UI 应显示“当前状态：已连接”和连接码。

9. 查询在线设备，确认返回内容包含 `authorizedDirectory`：

```powershell
Invoke-RestMethod http://localhost:8080/api/devices/online
```

如果使用 18081：

```powershell
Invoke-RestMethod http://localhost:18081/api/devices/online
```

10. 创建一个模拟任务，确认 Relay 下发的 `RemoteTask` 会携带 `authorizedDirectory`：

```powershell
$body = @{
  taskType = "LIST_DIR"
  payload = @{
    data = @{
      path = "src/main.java"
    }
  }
  timeoutSeconds = 30
} | ConvertTo-Json -Depth 5

Invoke-RestMethod -Method Post -Uri "http://localhost:8080/api/sessions/<sessionId>/tasks" -ContentType "application/json" -Body $body
```

11. 观察 Agent UI 日志，应看到任务信息和“任务授权目录”日志。

## 7. 启动/运行方式

构建：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

启动 Relay Server：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run
```

启动 Agent Client：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f agent-client\pom.xml javafx:run
```

运行后必须先在 Agent GUI 中选择授权目录，再连接服务器。

## 8. 遇到的问题与解决办法

问题 1：直接执行 `mvn clean package` 失败。

原因：当前 PowerShell PATH 没有 Maven。

解决办法：使用项目局部 Maven：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

问题 2：路径穿越必须被拒绝。

解决办法：`PathSandbox.resolveSecurely` 只接受相对路径，先用授权目录 resolve，再 normalize，最后检查结果是否仍以授权目录开头；`../../../etc/passwd` 会抛出 `SecurityException`。

问题 3：未选择授权目录不能连接。

解决办法：`AgentClientApplication` 在连接前检查 `authorizedDirectoryPath` 和 `pathSandbox`，缺失时直接在 UI 日志中提示并阻止连接。

问题 4：Relay 端不能接收缺失授权目录的 Agent 注册。

解决办法：`AgentConnectionController.hello` 校验 `authorizedDirectory`，为空时发送 `MISSING_AUTHORIZED_DIRECTORY` 错误并拒绝注册。

问题 5：本轮未做 GUI 端到端人工验证。

解决办法：本轮实际执行了单元测试和全量 Maven 构建；GUI 启动、在线设备查询和任务创建命令已在本文档中标注为“未执行，仅建议”。

## 9. 后续注意事项

- Phase 04 只完成授权目录与路径沙箱，不代表已经可以真实读取或写入文件。
- 后续所有真实文件读写必须调用 `PathSandbox.resolveSecurely`，不能直接拼接路径。
- 后续真实命令执行的 cwd 必须限制在授权目录内。
- `/api/devices/online` 返回的 `authorizedDirectory` 是 Agent 主动授权目录，Relay 只负责记录和转发，不得替用户扩大范围。
- Agent UI 必须始终可见，用户可一键断开。
- 不得实现隐藏运行、绕过授权、访问授权目录外文件、读取浏览器数据、读取 SSH 私钥、提权、修改注册表或规避审计日志。

---

# Phase 05 复现记录：list_dir / read_file 只读操作

## 1. 本次任务目标

根据 `tasks/phase-05-readonly-operations.md` 实现 Agent 端真实只读文件操作：

- `LIST_DIR` 返回授权目录内真实目录列表。
- `READ_FILE` 返回授权目录内真实文本文件内容。
- 所有路径必须经过 `PathSandbox` 校验。
- 任务必须异步执行，不能阻塞 JavaFX UI。
- 结果通过 STOMP `/app/agent/task-result` 返回给 Relay Server。
- 更新 `DEVELOPMENT_REPORT.md` 和本 `复现记录.md`。

## 2. 实际完成了什么

- 新增 `FileSystemService`，提供：
  - `listDirectory(String relativePath)`
  - `readFile(String relativePath)`
- 新增 `TaskExecutor`，按 `TaskType` 异步分发任务。
- `AgentConnectionClient` 已从模拟执行改为真实调用 `TaskExecutor`。
- `LIST_DIR` 输出真实 JSON 目录项，字段包含：
  - `name`
  - `size`
  - `modifiedTime`
  - `directory`
- `READ_FILE` 输出真实 JSON 文件信息和文本内容。
- 文本读取限制最大 1MB。
- 二进制文件不读取正文，只返回文件信息和说明。
- Agent UI 新增可折叠任务结果区域。
- Relay Server 继续保存真实任务结果到内存任务记录，`GET /api/tasks/{taskId}` 可返回 `summary/output/stderr/errorMessage`。
- 新增单元测试验证只读能力和路径安全。

## 3. 修改/新增了哪些文件

- `agent-client/pom.xml`
- `agent-client/src/main/java/com/airh/agent/filesystem/FileSystemService.java`
- `agent-client/src/main/java/com/airh/agent/executor/TaskExecutor.java`
- `agent-client/src/main/java/com/airh/agent/connection/AgentConnectionClient.java`
- `agent-client/src/main/java/com/airh/agent/connection/AgentConnectionListener.java`
- `agent-client/src/main/java/com/airh/agent/ui/AgentClientApplication.java`
- `agent-client/src/test/java/com/airh/agent/filesystem/FileSystemServiceTest.java`
- `relay-server/src/main/java/com/airh/relay/task/TaskService.java`
- `DEVELOPMENT_REPORT.md`
- `复现记录.md`

## 4. 使用过的关键命令

查看 Java 版本，已执行：

```powershell
cd E:\openclaw-project\ai-remote-helper
java -version
```

本机结果：

```text
openjdk version "21.0.9" 2025-10-21 LTS
OpenJDK Runtime Environment Temurin-21.0.9+10
OpenJDK 64-Bit Server VM Temurin-21.0.9+10
```

查看项目局部 Maven 版本，已执行：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -version
```

本机结果：

```text
Apache Maven 3.9.9
Java version: 21.0.9
OS name: "windows 11"
```

完整构建和测试，已执行：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

结果：`BUILD SUCCESS`。

## 5. 环境要求和版本信息

- 操作系统：Windows 11
- Java：OpenJDK 21.0.9 Temurin
- Maven：项目内置 Apache Maven 3.9.9
- Spring Boot：3.3.5
- JavaFX：21.0.5

如果当前终端没有全局 Maven，不要直接依赖 `mvn`，使用项目内置 Maven：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

## 6. 如何从零复现

1. 进入项目根目录：

```powershell
cd E:\openclaw-project\ai-remote-helper
```

2. 确认 Java 版本：

```powershell
java -version
```

要求 Java 21。

3. 执行全量构建：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

4. 确认输出中包含：

```text
BUILD SUCCESS
```

5. 启动 Relay Server，未执行，仅建议用于人工端到端验证：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run
```

6. 新开一个 PowerShell，启动 Agent Client，未执行，仅建议用于人工端到端验证：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f agent-client\pom.xml javafx:run
```

7. 在 Agent GUI 中手动选择授权目录，例如：

```text
E:\openclaw-project\ai-remote-helper
```

8. 在 Agent GUI 中连接：

```text
http://localhost:8080
```

9. 查询在线设备，未执行，仅建议：

```powershell
Invoke-RestMethod -Method Get -Uri "http://localhost:8080/api/devices/online"
```

10. 创建 `LIST_DIR` 任务，未执行，仅建议：

```powershell
$body = @{
  taskType = "LIST_DIR"
  payload = @{
    data = @{
      path = "."
    }
  }
  timeoutSeconds = 30
} | ConvertTo-Json -Depth 5

Invoke-RestMethod -Method Post -Uri "http://localhost:8080/api/sessions/<sessionId>/tasks" -ContentType "application/json" -Body $body
```

11. 查询任务结果，未执行，仅建议：

```powershell
Invoke-RestMethod -Method Get -Uri "http://localhost:8080/api/tasks/<taskId>"
```

预期 `output` 是 JSON 数组，每项包含 `name`、`size`、`modifiedTime`、`directory`。

12. 创建 `READ_FILE` 任务，未执行，仅建议：

```powershell
$body = @{
  taskType = "READ_FILE"
  payload = @{
    data = @{
      path = "README.md"
    }
  }
  timeoutSeconds = 30
} | ConvertTo-Json -Depth 5

Invoke-RestMethod -Method Post -Uri "http://localhost:8080/api/sessions/<sessionId>/tasks" -ContentType "application/json" -Body $body
```

13. 查询任务结果，未执行，仅建议：

```powershell
Invoke-RestMethod -Method Get -Uri "http://localhost:8080/api/tasks/<taskId>"
```

预期 `output` 是 JSON 对象，文本文件会包含 `contentReturned: true` 和 `content`。

14. 验证路径越界拒绝，未执行，仅建议：

```powershell
$body = @{
  taskType = "READ_FILE"
  payload = @{
    data = @{
      path = "../secret.txt"
    }
  }
  timeoutSeconds = 30
} | ConvertTo-Json -Depth 5

Invoke-RestMethod -Method Post -Uri "http://localhost:8080/api/sessions/<sessionId>/tasks" -ContentType "application/json" -Body $body
```

预期任务结果为 `FAILED`，错误信息包含路径越界或拒绝访问。

## 7. 启动/运行方式

构建：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

启动 Relay Server，未执行，仅建议：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run
```

启动 Agent Client，未执行，仅建议：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f agent-client\pom.xml javafx:run
```

运行后必须先在 Agent GUI 中选择授权目录，再连接服务器。

## 8. 遇到的问题与解决办法

问题 1：真实读取必须限制在授权目录内。

解决办法：`FileSystemService` 不直接拼接路径，所有入口都调用 `PathSandbox.resolveSecurely`，绝对路径和 `../` 越界路径都会被拒绝。

问题 2：大文件不能无限读取。

解决办法：`FileSystemService.MAX_TEXT_FILE_BYTES` 限制为 1MB，超过后只返回文件名、大小、修改时间、类型和说明，不返回正文。

问题 3：二进制文件不应该当文本返回。

解决办法：读取前检查前 8192 字节，遇到 NUL 字节或异常控制字符时判定为二进制，只返回文件信息，不返回正文。

问题 4：任务不能阻塞 UI。

解决办法：新增 `TaskExecutor`，使用后台线程池执行任务；`AgentConnectionClient` 收到 STOMP 任务后调用异步执行，再回传结果。

问题 5：Java 时间类型序列化为 JSON 需要额外模块。

解决办法：`agent-client/pom.xml` 增加 `jackson-datatype-jsr310`，`TaskExecutor` 注册 `JavaTimeModule`。

## 9. 后续注意事项

- Phase 05 只实现只读操作，不包含写文件、命令执行、补丁应用。
- 后续 Phase 06 写文件必须先备份，再写入。
- 后续所有文件路径仍必须经过 `PathSandbox`。
- `payload.data.path` 是当前推荐传参字段，同时兼容 `relativePath` 和 `relative_path`。
- Agent UI 必须始终可见，远程操作日志必须持续展示。
- 不得实现隐藏运行、绕过授权、访问授权目录外文件、读取浏览器数据、读取 SSH 私钥、提权、修改注册表或规避审计日志。

---

# Phase 06 复现记录：write_file / apply_patch 写入操作

## 1. 本次任务目标

根据 `tasks/phase-06-write-operations.md` 实现 Agent 端真实写入操作：

- `WRITE_FILE` 写入授权目录内文件。
- `APPLY_PATCH` 对授权目录内已有文件应用补丁。
- 写入已有文件前自动备份到 `.ai-remote-helper/backups/{timestamp}/`。
- 备份保留原目录结构。
- 每个文件最多保留最近 3 个备份版本。
- 所有写入路径必须经过 `PathSandbox` 校验。
- 写入后返回 diff 摘要。
- 更新 `DEVELOPMENT_REPORT.md` 和本 `复现记录.md`。

## 2. 实际完成了什么

- 新增 `BackupService`，负责备份、列出备份和清理旧备份。
- 扩展 `FileSystemService`：
  - `writeFile(relativePath, content)`：创建或覆盖 UTF-8 文本文件。
  - `applyPatch(relativePath, patch)`：对已有 UTF-8 文本文件应用 unified diff hunk。
  - 两个写入入口都调用 `PathSandbox.resolveSecurely`。
  - 修改已有文件前会备份原文件。
  - 写入后返回路径、文件大小、备份路径和 diff 摘要。
- 更新 `TaskExecutor`：
  - 支持 `WRITE_FILE`。
  - 支持 `APPLY_PATCH`。
  - `WRITE_FILE` 从 `payload.data.content` 或 `payload.data.text` 读取内容。
  - `APPLY_PATCH` 从 `payload.data.patch` 或 `payload.data.diff` 读取补丁。
- 更新 `FileSystemServiceTest`，覆盖新建文件、备份、备份保留 3 版、越界拒绝、补丁应用。
- 已执行全量 Maven 构建和测试，结果为 `BUILD SUCCESS`。

## 3. 修改/新增了哪些文件

```text
agent-client/src/main/java/com/airh/agent/filesystem/BackupService.java
agent-client/src/main/java/com/airh/agent/filesystem/FileSystemService.java
agent-client/src/main/java/com/airh/agent/executor/TaskExecutor.java
agent-client/src/test/java/com/airh/agent/filesystem/FileSystemServiceTest.java
DEVELOPMENT_REPORT.md
复现记录.md
```

## 4. 使用过的关键命令

查看 Java 版本，已执行：

```powershell
cd E:\openclaw-project\ai-remote-helper
java -version
```

本机结果：

```text
openjdk version "21.0.9" 2025-10-21 LTS
OpenJDK Runtime Environment Temurin-21.0.9+10
OpenJDK 64-Bit Server VM Temurin-21.0.9+10
```

查看项目局部 Maven 版本，已执行：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -version
```

本机结果：

```text
Apache Maven 3.9.9
Java version: 21.0.9
OS name: "windows 11"
```

完整构建和测试，已执行：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

结果：`BUILD SUCCESS`。

查看变更状态，已执行：

```powershell
cd E:\openclaw-project\ai-remote-helper
git status --short
```

启动 Relay Server，本轮未执行，仅建议用于人工端到端验证：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run
```

启动 Agent Client，本轮未执行，仅建议用于人工端到端验证：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f agent-client\pom.xml javafx:run
```

## 5. 环境要求和版本信息

- 操作系统：Windows 11
- Shell：PowerShell
- Java：OpenJDK 21.0.9 Temurin
- Maven：项目内置 Apache Maven 3.9.9
- Spring Boot：3.3.5
- JavaFX：21.0.5
- JUnit Jupiter：5.10.5

如果当前终端没有全局 Maven，使用项目内置 Maven：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

## 6. 如何从零复现

1. 进入项目根目录：

```powershell
cd E:\openclaw-project\ai-remote-helper
```

2. 确认 Java 21 可用：

```powershell
java -version
```

3. 确认项目内置 Maven 可用：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -version
```

4. 执行全量构建和测试：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

5. 确认测试结果包含：

```text
Running com.airh.agent.filesystem.FileSystemServiceTest
Tests run: 8, Failures: 0, Errors: 0, Skipped: 0
Running com.airh.agent.safety.PathSandboxTest
Tests run: 2, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

6. 启动 Relay Server，未执行，仅建议：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run
```

7. 新开 PowerShell 启动 Agent Client，未执行，仅建议：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f agent-client\pom.xml javafx:run
```

8. 在 Agent GUI 中手动选择授权目录，例如：

```text
E:\openclaw-project\ai-remote-helper
```

9. 在 Agent GUI 中连接 Relay：

```text
http://localhost:8080
```

10. 查询在线设备获取 `sessionId`，未执行，仅建议：

```powershell
Invoke-RestMethod -Method Get -Uri "http://localhost:8080/api/devices/online"
```

11. 创建 `WRITE_FILE` 任务，未执行，仅建议：

```powershell
$body = @{
  taskType = "WRITE_FILE"
  payload = @{
    data = @{
      path = "tmp/write-demo.txt"
      content = "第一行`n第二行`n"
    }
  }
  timeoutSeconds = 30
} | ConvertTo-Json -Depth 5

Invoke-RestMethod -Method Post -Uri "http://localhost:8080/api/sessions/<sessionId>/tasks" -ContentType "application/json" -Body $body
```

12. 再次创建同一路径的 `WRITE_FILE` 任务，未执行，仅建议：

```powershell
$body = @{
  taskType = "WRITE_FILE"
  payload = @{
    data = @{
      path = "tmp/write-demo.txt"
      content = "第一行已修改`n第二行`n第三行`n"
    }
  }
  timeoutSeconds = 30
} | ConvertTo-Json -Depth 5

Invoke-RestMethod -Method Post -Uri "http://localhost:8080/api/sessions/<sessionId>/tasks" -ContentType "application/json" -Body $body
```

预期：授权目录下出现 `.ai-remote-helper/backups/{timestamp}/tmp/write-demo.txt`，任务结果包含 `backupPath` 和 diff 摘要。

13. 创建 `APPLY_PATCH` 任务，未执行，仅建议：

```powershell
$patch = @"
--- a/tmp/write-demo.txt
+++ b/tmp/write-demo.txt
@@ -1,3 +1,3 @@
-第一行已修改
+第一行通过补丁修改
 第二行
 第三行
"@

$body = @{
  taskType = "APPLY_PATCH"
  payload = @{
    data = @{
      path = "tmp/write-demo.txt"
      patch = $patch
    }
  }
  timeoutSeconds = 30
} | ConvertTo-Json -Depth 5

Invoke-RestMethod -Method Post -Uri "http://localhost:8080/api/sessions/<sessionId>/tasks" -ContentType "application/json" -Body $body
```

14. 查询任务结果，未执行，仅建议：

```powershell
Invoke-RestMethod -Method Get -Uri "http://localhost:8080/api/tasks/<taskId>"
```

预期：任务状态为 `SUCCESS`，`summary` 包含“文件写入成功”或“补丁应用成功”，`output` 中包含 `diffSummary`。

15. 验证越界写入会失败，未执行，仅建议：

```powershell
$body = @{
  taskType = "WRITE_FILE"
  payload = @{
    data = @{
      path = "../outside.txt"
      content = "不应写入"
    }
  }
  timeoutSeconds = 30
} | ConvertTo-Json -Depth 5

Invoke-RestMethod -Method Post -Uri "http://localhost:8080/api/sessions/<sessionId>/tasks" -ContentType "application/json" -Body $body
```

预期：任务状态为 `FAILED`，错误信息包含路径越界或拒绝访问。

## 7. 启动/运行方式

构建：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

启动 Relay Server，未执行，仅建议：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run
```

启动 Agent Client，未执行，仅建议：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f agent-client\pom.xml javafx:run
```

运行后必须先在 Agent GUI 中选择授权目录，再连接服务器。远程写入只允许发生在授权目录内。

## 8. 遇到的问题与解决办法

问题 1：写入已有文件前必须保留可恢复版本。

解决办法：新增 `BackupService.backupFile(Path target)`，在写入前复制原文件到 `.ai-remote-helper/backups/{timestamp}/{relativePath}`，并保留原相对目录结构。

问题 2：备份不能无限增长。

解决办法：每次写入后调用 `BackupService.cleanupOldBackups(relativePath, 3)`，同一文件只保留最近 3 个备份版本。

问题 3：写入路径必须限制在授权目录内。

解决办法：`writeFile` 和 `applyPatch` 都先调用 `PathSandbox.resolveSecurely`，绝对路径和 `../` 越界路径会抛出 `SecurityException`。

问题 4：补丁格式不能过度扩展。

解决办法：当前阶段只实现基础 unified diff hunk 解析，支持 `@@ ... @@`、空格上下文行、`-` 删除行和 `+` 新增行；不声明完整兼容 `git apply`。

问题 5：任务结果要能给 Relay 侧保存。

解决办法：`TaskExecutor` 把 `WriteFileResult` 序列化为 JSON，包含 `path`、`size`、`backupPath`、`diffSummary` 和 `message`。

## 9. 后续注意事项

- `WRITE_FILE` 和 `APPLY_PATCH` 当前按 UTF-8 文本处理，不支持二进制写入。
- `APPLY_PATCH` 当前是基础 unified diff 解析器，不是完整 `git apply`。
- 本阶段未实现 JavaFX UI 中的独立 diff 预览区；diff 摘要通过任务日志和任务结果返回。
- 后续命令执行阶段必须继续遵守授权目录、可见 UI、一键断开、危险命令拦截、任务超时和审计日志要求。
- 不得实现隐藏运行、绕过授权、访问授权目录外文件、读取浏览器数据、读取 SSH 私钥、提权、修改注册表或规避审计日志。

# Phase 07 run_command 命令执行复现记录

## 1. 本次任务目标

实现 `tasks/phase-07-command-execution.md` 中 Agent 端 `RUN_COMMAND` 能力：

- 使用 `ProcessBuilder` 执行命令。
- stdout 和 stderr 分开捕获。
- 支持工作目录，默认是用户授权目录。
- 支持超时，默认 30 秒，最大 300 秒。
- 支持 `kill()` 终止运行中的进程和子进程。
- 执行期间通过 callback 流式输出日志。
- `TaskExecutor` 能分发 `RUN_COMMAND`，执行中上报 `TASK_LOG`，执行结束返回 `CommandResult`。
- 增加命令执行单元测试。

## 2. 实际完成了什么

- 新增 `CommandExecutionService`：
  - Windows 下通过 `cmd.exe /c <command>` 执行。
  - 非 Windows 下通过 `sh -c <command>` 执行。
  - stdin 在进程启动后立即关闭，因此不支持交互式命令。
  - 默认 cwd 为授权目录。
  - `workingDir` 可以是授权目录内相对路径，也可以是授权目录内绝对路径。
  - `workingDir` 越界时抛出 `SecurityException`。
  - stdout/stderr 使用独立 reader 读取，分别累积为完整字符串。
  - stdout/stderr chunk 通过 callback 实时传出。
  - 超时后调用 `kill()`，强杀当前进程及其 descendants。
- 新增 `CommandResult`：
  - `exitCode`
  - `stdout`
  - `stderr`
  - `durationMs`
  - `timedOut`
  - `killed`
- 更新 `TaskExecutor`：
  - 支持 `RUN_COMMAND`。
  - 支持从以下 payload 形态读取参数：

```json
{
  "data": {
    "command": "echo hello",
    "cwd": ".",
    "timeoutSeconds": 30
  }
}
```

  - 也支持顶层字段 `command`、`workingDir`、`timeoutSeconds`。
  - stdout chunk 会上报为 `[stdout] ...` 任务日志。
  - stderr chunk 会上报为 `[stderr] ...` 任务日志。
  - 命令完成后把 `CommandResult` JSON 作为 `TaskResultMessage.output` 返回。
  - exitCode 为 0 且未超时、未 kill 时返回 `SUCCESS`；否则返回 `FAILED`。
  - 命中 `CommandRiskDetector` 的 `BLOCKED` 风险时拒绝执行。
- 更新 `AgentConnectionClient`：
  - 连接时创建同一个 `PathSandbox`，同时传给 `FileSystemService` 和 `CommandExecutionService`。
- 新增 `CommandExecutionServiceTest`：
  - echo 命令成功。
  - sleep/ping 超时后被 kill。
  - cwd 生效。
  - 非 0 exit code 能正确捕获。

## 3. 修改/新增了哪些文件

- `agent-client/src/main/java/com/airh/agent/executor/CommandExecutionService.java`
- `agent-client/src/main/java/com/airh/agent/executor/CommandResult.java`
- `agent-client/src/main/java/com/airh/agent/executor/TaskExecutor.java`
- `agent-client/src/main/java/com/airh/agent/connection/AgentConnectionClient.java`
- `agent-client/src/test/java/com/airh/agent/executor/CommandExecutionServiceTest.java`
- `DEVELOPMENT_REPORT.md`
- `复现记录.md`

## 4. 使用过的关键命令

查看 Java 版本：

```powershell
cd E:\openclaw-project\ai-remote-helper
java -version
```

实际结果：

```text
openjdk version "21.0.9" 2025-10-21 LTS
OpenJDK Runtime Environment Temurin-21.0.9+10
OpenJDK 64-Bit Server VM Temurin-21.0.9+10
```

查看 Maven 版本：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -version
```

实际结果：

```text
Apache Maven 3.9.9
Java version: 21.0.9
OS name: "windows 11", arch: "amd64"
```

执行全量构建和测试：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

实际结果：`BUILD SUCCESS`。

关键测试结果：

```text
CommandExecutionServiceTest: Tests run: 4, Failures: 0, Errors: 0, Skipped: 0
FileSystemServiceTest: Tests run: 8, Failures: 0, Errors: 0, Skipped: 0
PathSandboxTest: Tests run: 2, Failures: 0, Errors: 0, Skipped: 0
全部测试: Tests run: 14, Failures: 0, Errors: 0, Skipped: 0
```

## 5. 环境要求和版本信息

- 操作系统：Windows 11
- Java：OpenJDK 21.0.9 Temurin
- Maven：项目内置 `.tools/apache-maven-3.9.9`
- 项目 Java 版本：21
- Spring Boot：3.3.5
- JavaFX：21.0.5

必须先保证本机有 Java 21，并优先使用项目内置 Maven：

```powershell
cd E:\openclaw-project\ai-remote-helper
java -version
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -version
```

## 6. 如何从零复现

1. 进入项目根目录：

```powershell
cd E:\openclaw-project\ai-remote-helper
```

2. 确认阶段文件要求：

```powershell
Get-Content -Raw tasks\phase-07-command-execution.md
```

3. 确认 Agent 端任务分发器位置：

```powershell
Get-Content -Raw agent-client\src\main\java\com\airh\agent\executor\TaskExecutor.java
```

4. 新增命令执行服务文件：

```powershell
notepad agent-client\src\main\java\com\airh\agent\executor\CommandExecutionService.java
```

把本次实现写入该文件。注意必须使用 `ProcessBuilder`，并通过 `PathSandbox` 限制工作目录。

5. 新增命令结果 record：

```powershell
notepad agent-client\src\main\java\com\airh\agent\executor\CommandResult.java
```

字段必须包含：

```java
int exitCode, String stdout, String stderr, long durationMs, boolean timedOut, boolean killed
```

6. 修改 `TaskExecutor`：

```powershell
notepad agent-client\src\main\java\com\airh\agent\executor\TaskExecutor.java
```

需要增加：

- `CommandExecutionService` 依赖。
- `RUN_COMMAND` switch 分支。
- `command`、`workingDir/cwd`、`timeoutSeconds/timeout` 参数解析。
- stdout/stderr chunk 到 `taskLogSink` 的转发。
- `CommandResult` JSON 输出。

7. 修改 `AgentConnectionClient`：

```powershell
notepad agent-client\src\main\java\com\airh\agent\connection\AgentConnectionClient.java
```

让 `FileSystemService` 和 `CommandExecutionService` 共享同一个 `PathSandbox`。

8. 新增单元测试：

```powershell
notepad agent-client\src\test\java\com\airh\agent\executor\CommandExecutionServiceTest.java
```

至少覆盖：

- echo 成功。
- 超时。
- 工作目录。
- exit code。

9. 执行全量构建：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

预期：`BUILD SUCCESS`。

10. 查看当前修改：

```powershell
git status --short
```

预期能看到本阶段新增/修改文件。

## 7. 启动/运行方式

构建项目：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

启动 Relay Server，未执行，仅建议：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run
```

启动 Agent Client，未执行，仅建议：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f agent-client\pom.xml javafx:run
```

运行后必须在 Agent GUI 中手动选择授权目录，再连接 Relay Server。`RUN_COMMAND` 的 cwd 默认就是这个授权目录。

创建命令任务，未执行，仅建议：

```powershell
$body = @{
  taskType = "RUN_COMMAND"
  payload = @{
    data = @{
      command = "echo hello-airh"
      cwd = "."
      timeoutSeconds = 30
    }
  }
  timeoutSeconds = 30
} | ConvertTo-Json -Depth 5

Invoke-RestMethod -Method Post -Uri "http://localhost:8080/api/sessions/<sessionId>/tasks" -ContentType "application/json" -Body $body
```

预期：

- Agent 日志显示收到任务。
- Relay 收到 `/app/agent/task-log` 日志。
- stdout 日志包含 `[stdout] hello-airh`。
- 任务结果 output 是 `CommandResult` JSON。

## 8. 遇到的问题与解决办法

问题 1：命令必须限制在授权目录内。

解决办法：`CommandExecutionService` 使用 `PathSandbox` 解析工作目录。默认 cwd 是 `authorizedDirectory`；相对路径通过 `resolveSecurely` 校验；绝对路径必须 `startsWith(authorizedDirectory)`。

问题 2：stdout 和 stderr 必须分开捕获，同时还要实时上报。

解决办法：启动进程后分别读取 `process.getInputStream()` 和 `process.getErrorStream()`，两个 stream 分别追加到不同 `StringBuilder`，同时调用不同 callback。`TaskExecutor` 中分别加 `[stdout]`、`[stderr]` 前缀后上报任务日志。

问题 3：超时后不能只结束父进程。

解决办法：`kill()` 先遍历 `process.descendants()` 并 `destroyForcibly()`，再对当前进程 `destroyForcibly()`。

问题 4：命令不能卡在交互输入。

解决办法：进程启动后立即关闭 `process.getOutputStream()`，即关闭 stdin。

问题 5：Windows 和 Linux/macOS shell 不一样。

解决办法：根据 `os.name` 判断。Windows 使用 `cmd.exe /c`，非 Windows 使用 `sh -c`。测试中的 sleep、pwd、exit code 命令也按系统分支处理。

## 9. 后续注意事项

- 当前 `kill()` 针对 `CommandExecutionService` 内部当前进程；不要并发下发多个长时间命令任务给同一个 Agent。
- 命令输出读取使用 JVM 默认字符集；如果后续要稳定处理中文输出，需要明确 shell 编码策略。
- 本阶段只实现 Agent 端命令执行和日志上报；Relay 端任务日志分页存储还未实现。
- 本阶段未自动启动 JavaFX GUI 做端到端人工验证。
- 所有命令必须继续遵守授权目录、可见 UI、一键断开、危险命令拦截、超时和审计要求。
- 不得实现隐藏运行、绕过授权、访问授权目录外文件、读取浏览器数据、读取 SSH 私钥、提权、修改注册表或规避审计日志。

# Phase 08 危险命令拦截和敏感文件保护复现记录

## 1. 本次任务目标

实现 `tasks/phase-08-safety-interception.md` 中本轮用户明确指定的安全拦截能力：

- 增强 `common-safety` 中的 `CommandRiskDetector`，覆盖 `BLOCKED`、`HIGH`、`MEDIUM` 三类命令风险规则。
- 新增 `SensitiveFileProtector`，通过 `checkPath(path)` 返回 `RiskLevel`。
- 将 `SensitiveFileProtector` 接入 `FileSystemService` 的 `readFile`、`writeFile`、`applyPatch`。
- 为 `CommandRiskDetector` 和 `SensitiveFileProtector` 创建测试。
- 执行 `.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package`。
- 更新 `DEVELOPMENT_REPORT.md` 和本文件。

## 2. 实际完成了什么

已完成：

- `CommandRiskDetector` 改为正则规则检测。
- `BLOCKED` 命令包括：
  - `rm -rf /`
  - `format`
  - `del /f /s`
  - `reg delete` / `registry delete`
  - `shutdown`
  - `mkfs`
  - `dd if=... of=/dev/...`
  - `> /dev/sda` 等磁盘设备重定向
  - `curl ... | sh/bash`
  - `wget ... | sh/bash`
- `HIGH` 命令包括：
  - `sudo`
  - `su -`
  - `chmod 777`
  - `chown`
  - 非根目录 `rm -rf`
  - `netsh`
  - `sc stop`
  - `sc delete`
  - `taskkill /f`
- `MEDIUM` 命令包括：
  - `rm`
  - `del`
  - `kill`
  - `service ... stop`
- 新增 `SensitiveFileProtector`：
  - `BLOCKED`：`~/.ssh/`、`~/.gnupg/`、Chrome/Firefox/Edge profile、`/etc/passwd`、`/etc/shadow`、`/etc/sudoers`、Windows `System32\config\SAM/SECURITY/SYSTEM`。
  - `HIGH`：`~/.bashrc`、`~/.zshrc`、`~/.profile`、Windows Startup 目录、hosts 文件。
- `SensitiveFileGuard` 继续保留旧接口，并复用新的 `SensitiveFileProtector`。
- `FileSystemService` 在路径沙箱解析后检查敏感路径：
  - `BLOCKED`：抛出 `SecurityException`。
  - `HIGH`：记录 warning 后继续执行。
- 新增 `common-safety` 测试，并扩展 `agent-client` 文件服务测试。
- 已执行全量 Maven 构建和测试，结果 `BUILD SUCCESS`。

未完成，原因是本轮用户没有要求实现：

- `SafetyChecker` 统一服务，未执行，仅建议后续补。
- `safety-rules.json` 可配置规则，未执行，仅建议后续补。
- Agent UI 拦截结果展示，未执行，仅建议后续补。
- Relay Server 审计事件 DTO 和持久化，未执行，仅建议后续补。

## 3. 修改/新增了哪些文件

修改：

```text
common-safety/pom.xml
common-safety/src/main/java/com/airh/safety/CommandRiskDetector.java
common-safety/src/main/java/com/airh/safety/SensitiveFileGuard.java
agent-client/src/main/java/com/airh/agent/filesystem/FileSystemService.java
agent-client/src/test/java/com/airh/agent/filesystem/FileSystemServiceTest.java
DEVELOPMENT_REPORT.md
复现记录.md
```

新增：

```text
common-safety/src/main/java/com/airh/safety/SensitiveFileProtector.java
common-safety/src/test/java/com/airh/safety/CommandRiskDetectorTest.java
common-safety/src/test/java/com/airh/safety/SensitiveFileProtectorTest.java
```

## 4. 使用过的关键命令

读取任务说明：

```powershell
cd E:\openclaw-project\ai-remote-helper
Get-Content -Raw tasks\phase-08-safety-interception.md
```

查看项目文件：

```powershell
cd E:\openclaw-project\ai-remote-helper
rg --files
Get-ChildItem -Force
```

查看现有安全类和风险枚举：

```powershell
cd E:\openclaw-project\ai-remote-helper
Get-Content -Raw common-safety\src\main\java\com\airh\safety\CommandRiskDetector.java
Get-Content -Raw common-safety\src\main\java\com\airh\safety\SensitiveFileGuard.java
Get-Content -Raw common-safety\src\main\java\com\airh\safety\PathGuard.java
Get-Content -Raw common-protocol\src\main\java\com\airh\protocol\enums\RiskLevel.java
```

查看文件服务和测试：

```powershell
cd E:\openclaw-project\ai-remote-helper
Get-Content -Raw agent-client\src\main\java\com\airh\agent\filesystem\FileSystemService.java
Get-Content -Raw agent-client\src\test\java\com\airh\agent\filesystem\FileSystemServiceTest.java
```

查看命令执行接入点：

```powershell
cd E:\openclaw-project\ai-remote-helper
Get-Content -Raw agent-client\src\main\java\com\airh\agent\executor\TaskExecutor.java
```

查找引用：

```powershell
cd E:\openclaw-project\ai-remote-helper
rg "CommandRiskDetector|SensitiveFileGuard|RiskLevel" -n
```

查看 Java 和 Maven 版本：

```powershell
cd E:\openclaw-project\ai-remote-helper
java -version
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -version
```

执行全量构建和测试：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

查看 Git 修改状态：

```powershell
cd E:\openclaw-project\ai-remote-helper
git status --short
```

## 5. 环境要求和版本信息

本次实际验证环境：

```text
操作系统：Windows 11 10.0 amd64
Java：OpenJDK 21.0.9 Temurin
Maven：Apache Maven 3.9.9
Maven 路径：E:\openclaw-project\ai-remote-helper\.tools\apache-maven-3.9.9
Spring Boot：3.3.5
JavaFX：21.0.5
项目目录：E:\openclaw-project\ai-remote-helper
```

实际版本命令输出摘要：

```text
openjdk version "21.0.9" 2025-10-21 LTS
Apache Maven 3.9.9
Default locale: zh_CN, platform encoding: UTF-8
OS name: "windows 11", version: "10.0", arch: "amd64"
```

## 6. 如何从零复现

1. 进入项目根目录：

```powershell
cd E:\openclaw-project\ai-remote-helper
```

2. 确认任务文件：

```powershell
Get-Content -Raw tasks\phase-08-safety-interception.md
```

3. 修改 `CommandRiskDetector`：

```powershell
notepad common-safety\src\main\java\com\airh\safety\CommandRiskDetector.java
```

复现要点：

- 引入 `java.util.regex.Pattern`。
- 定义 `BLOCKED_PATTERNS`、`HIGH_PATTERNS`、`MEDIUM_PATTERNS`。
- `detect(String command)` 先标准化大小写、反斜杠和空白，再按 `BLOCKED -> HIGH -> MEDIUM -> LOW` 顺序返回风险等级。

4. 新增 `SensitiveFileProtector`：

```powershell
notepad common-safety\src\main\java\com\airh\safety\SensitiveFileProtector.java
```

复现要点：

- 包名为 `com.airh.safety`。
- 提供：

```java
public RiskLevel checkPath(String path)
public RiskLevel checkPath(Path path)
```

- `~` 要扩展为 `System.getProperty("user.home")`。
- Windows 反斜杠统一转为 `/`。
- 返回值只用现有 `RiskLevel` 枚举：`LOW`、`HIGH`、`BLOCKED`。

5. 更新 `SensitiveFileGuard`：

```powershell
notepad common-safety\src\main\java\com\airh\safety\SensitiveFileGuard.java
```

复现要点：

- 保留 `isSensitive(Path path)`。
- 内部调用 `SensitiveFileProtector.checkPath(path)`。
- 命中 `BLOCKED` 时返回 `true`。

6. 更新 `common-safety/pom.xml`：

```powershell
notepad common-safety\pom.xml
```

复现要点：

- 增加 JUnit 5 测试依赖。
- 增加 Maven Surefire 3.2.5 插件。

7. 更新 `FileSystemService`：

```powershell
notepad agent-client\src\main\java\com\airh\agent\filesystem\FileSystemService.java
```

复现要点：

- 新增字段：

```java
private final SensitiveFileProtector sensitiveFileProtector;
```

- 构造函数中初始化。
- 在 `readFile`、`writeFile`、`applyPatch` 中，先通过 `PathSandbox` 得到授权目录内真实路径，再调用敏感路径检查。
- `BLOCKED` 抛 `SecurityException`。
- `HIGH` 用 logger warning 记录，但继续执行。

8. 新增 `CommandRiskDetectorTest`：

```powershell
notepad common-safety\src\test\java\com\airh\safety\CommandRiskDetectorTest.java
```

至少覆盖：

- `rm -rf /` 返回 `BLOCKED`。
- `format C:` 返回 `BLOCKED`。
- `curl ... | sh` 返回 `BLOCKED`。
- `sudo` 返回 `HIGH`。
- `rm -rf build` 返回 `HIGH`。
- 普通 `rm temp.txt` 返回 `MEDIUM`。
- `echo hello` 返回 `LOW`。

9. 新增 `SensitiveFileProtectorTest`：

```powershell
notepad common-safety\src\test\java\com\airh\safety\SensitiveFileProtectorTest.java
```

至少覆盖：

- `~/.ssh/id_rsa` 返回 `BLOCKED`。
- `~/.gnupg/...` 返回 `BLOCKED`。
- Chrome/Firefox/Edge profile 返回 `BLOCKED`。
- `/etc/shadow` 和 Windows `System32\config\SAM` 返回 `BLOCKED`。
- `~/.bashrc`、`/etc/hosts`、Windows Startup 目录返回 `HIGH`。
- 普通项目文件返回 `LOW`。

10. 扩展 `FileSystemServiceTest`：

```powershell
notepad agent-client\src\test\java\com\airh\agent\filesystem\FileSystemServiceTest.java
```

至少覆盖：

- 授权目录内 `.ssh/id_rsa` 读取被拒绝。
- 授权目录内 `.gnupg/private.key` 写入被拒绝。
- 授权目录内 `.ssh/config` 应用补丁被拒绝。

11. 执行全量构建：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

预期结果：

```text
BUILD SUCCESS
common-safety Tests run: 9, Failures: 0, Errors: 0, Skipped: 0
agent-client Tests run: 17, Failures: 0, Errors: 0, Skipped: 0
```

12. 查看修改：

```powershell
cd E:\openclaw-project\ai-remote-helper
git status --short
```

## 7. 启动/运行方式

本阶段主要是安全规则和文件服务拦截逻辑，已通过 Maven 测试验证。构建命令：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

启动 Relay Server，未执行，仅建议：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run
```

启动 Agent Client，未执行，仅建议：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f agent-client\pom.xml javafx:run
```

手工验证敏感文件读取拦截，未执行，仅建议：

1. Agent GUI 手动选择一个测试授权目录。
2. 在该授权目录下创建 `.ssh\id_rsa` 测试文件。
3. 通过 Relay 下发 `READ_FILE` 任务，路径填 `.ssh/id_rsa`。
4. 预期 Agent 返回失败，错误包含 `敏感文件阻断规则`。

示例任务，未执行，仅建议：

```powershell
$body = @{
  taskType = "READ_FILE"
  payload = @{
    data = @{
      path = ".ssh/id_rsa"
    }
  }
  timeoutSeconds = 30
} | ConvertTo-Json -Depth 5

Invoke-RestMethod -Method Post -Uri "http://localhost:8080/api/sessions/<sessionId>/tasks" -ContentType "application/json" -Body $body
```

## 8. 遇到的问题与解决办法

问题 1：只检查授权目录外路径不够，授权目录内部也可能包含 `.ssh`、`.gnupg` 或浏览器 profile。

解决办法：`FileSystemService` 先用 `PathSandbox` 保证路径不越界，再用 `SensitiveFileProtector` 检查授权目录内的真实路径。这样即使用户误把 home 目录授权给 Agent，敏感子目录仍会被拦截。

问题 2：`HIGH` 路径应该警告但不能直接阻断。

解决办法：`checkSensitivePath` 对 `RiskLevel.HIGH` 使用 `Logger.warning` 记录，并继续执行；对 `RiskLevel.BLOCKED` 才抛出 `SecurityException`。

问题 3：Windows 和 Linux/macOS 路径格式不同。

解决办法：`SensitiveFileProtector` 把 `\` 统一转成 `/`，再转小写比较；`~` 路径会用 `System.getProperty("user.home")` 展开。

问题 4：命令风险规则不能只用简单 `contains`。

解决办法：`CommandRiskDetector` 改为按风险分组的正则列表，并按 `BLOCKED -> HIGH -> MEDIUM -> LOW` 顺序判断，避免 `rm -rf /` 被普通 `rm` 提前判成中风险。

问题 5：`common-safety` 之前没有测试配置。

解决办法：在 `common-safety/pom.xml` 增加 `junit-jupiter` 和 `maven-surefire-plugin`，让该模块能运行 JUnit 5 测试。

## 9. 后续注意事项

- 本轮没有实现 `safety-rules.json`，规则仍写在 Java 代码里。
- 本轮没有实现 `SafetyChecker` 统一 `ALLOW / WARN / DENY` 返回结构。
- 本轮没有把 `HIGH` 和 `BLOCKED` 事件上报到 Relay Server 审计日志。
- 本轮没有修改 Agent UI 来显示专门的拦截结果。
- 当前命令检测是黑名单正则方式，不是完整 shell AST 解析；复杂转义、变量拼接和多层 shell 包装后续仍需增强。
- 后续实现 Relay 审计时，应把敏感路径拦截、危险命令拦截、HIGH 风险放行都写入审计日志。
- 不得因为测试方便而放宽授权目录、敏感文件保护、危险命令拦截或可见 Agent UI 的安全边界。

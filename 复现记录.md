# Phase 04 复现记录：授权目录选择与路径沙箱

## 1. 本次任务目标

根据 `tasks/phase-04-path-sandbox.md` 完成 Phase 04：Agent 启动后必须先由用户手动选择授权目录，后续路径都必须限制在授权目录内；授权目录需要通过连接协议传递给 Relay Server，并在任务下发时携带给 Agent。

本阶段不实现真实文件读取、真实文件写入、真实命令执行或后续阶段能力。

## 2. 实际完成了什么

- 新增 `AuthorizedDirectoryChooser`，封装 JavaFX 目录选择器。
- 新增 `PathSandbox`，支持授权目录内路径校验、安全解析相对路径和路径标准化。
- 更新 `AgentClientApplication`，选择目录后创建路径沙箱，未选择授权目录时禁止连接。
- `AgentConnectionClient` 连接时继续发送 `authorizedDirectory`，收到任务时记录服务端下发的授权目录。
- Relay Server 在 hello 阶段校验 `authorizedDirectory`，缺失时拒绝注册。
- `DeviceRegistry` 记录授权目录，并新增 `getAuthorizedDirectory(deviceId)`。
- `RemoteTask` 增加 `authorizedDirectory` 字段，`TaskService` 下发任务时携带授权目录。
- 新增 `PathSandboxTest`，验证正常相对路径允许、路径穿越拒绝。
- 已执行全量 Maven 构建检查，实际结果为 `BUILD SUCCESS`。

## 3. 修改/新增了哪些文件

```text
agent-client/pom.xml
agent-client/src/main/java/com/airh/agent/safety/PathSandbox.java
agent-client/src/main/java/com/airh/agent/ui/AuthorizedDirectoryChooser.java
agent-client/src/main/java/com/airh/agent/ui/AgentClientApplication.java
agent-client/src/main/java/com/airh/agent/connection/AgentConnectionClient.java
agent-client/src/test/java/com/airh/agent/safety/PathSandboxTest.java
common-protocol/src/main/java/com/airh/protocol/dto/RemoteTask.java
relay-server/src/main/java/com/airh/relay/device/DeviceRegistry.java
relay-server/src/main/java/com/airh/relay/task/TaskService.java
relay-server/src/main/java/com/airh/relay/websocket/AgentConnectionController.java
DEVELOPMENT_REPORT.md
复现记录.md
```

## 4. 使用过的关键命令

进入项目目录：

```powershell
cd E:\openclaw-project\ai-remote-helper
```

按要求执行 Maven 构建：

```powershell
mvn clean package
```

实际结果：失败。当前 PowerShell PATH 中没有 `mvn`，报错为 `The term 'mvn' is not recognized`。

使用项目局部 Maven 执行同等构建：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

实际结果：成功，Maven 输出 `BUILD SUCCESS`。

查看 Java 版本：

```powershell
java -version
```

查看项目局部 Maven 版本：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -version
```

查看变更状态：

```powershell
git status --short
git diff --stat
```

启动 relay-server，本轮未执行，仅建议：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run
```

启动 agent-client GUI，本轮未执行，仅建议：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f agent-client\pom.xml javafx:run
```

查询在线设备，本轮未执行，仅建议：

```powershell
Invoke-RestMethod http://localhost:8080/api/devices/online
```

创建模拟任务，本轮未执行，仅建议：

```powershell
$body = @{
  taskType = "LIST_DIR"
  payload = @{
    data = @{
      path = "src/main.java"
    }
  }
  timeoutSeconds = 30
} | ConvertTo-Json -Depth 5

Invoke-RestMethod -Method Post -Uri "http://localhost:8080/api/sessions/<sessionId>/tasks" -ContentType "application/json" -Body $body
```

## 5. 环境要求和版本信息

- 操作系统：Windows 11
- Shell：PowerShell
- Java：OpenJDK 21.0.9 Temurin
- Maven：项目局部 Apache Maven 3.9.9
- Spring Boot：3.3.5
- JavaFX：21.0.5
- JUnit Jupiter：5.10.5
- Maven Surefire Plugin：3.2.5

## 6. 如何从零复现

1. 进入项目目录：

```powershell
cd E:\openclaw-project\ai-remote-helper
```

2. 确认 Java 21 可用：

```powershell
java -version
```

3. 确认项目局部 Maven 可用：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -version
```

4. 执行全量构建和测试：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

5. 确认测试结果中包含：

```text
Running com.airh.agent.safety.PathSandboxTest
Tests run: 2, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

6. 启动 relay-server：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run
```

如果 8080 端口被占用，使用临时端口：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run -Dspring-boot.run.arguments=--server.port=18081
```

7. 另开一个 PowerShell，启动 Agent GUI：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f agent-client\pom.xml javafx:run
```

8. 在 Agent GUI 中操作：

- 服务器地址填写 `http://localhost:8080`；如果 Relay 使用 18081，则填写 `http://localhost:18081`。
- 先点击“选择授权目录”，手动选择一个工作目录。
- 再点击“连接”。
- 如果未选择授权目录直接点击“连接”，UI 应提示“连接失败：请先手动选择授权目录”。
- 连接成功后 UI 应显示“当前状态：已连接”和连接码。

9. 查询在线设备，确认返回内容包含 `authorizedDirectory`：

```powershell
Invoke-RestMethod http://localhost:8080/api/devices/online
```

如果使用 18081：

```powershell
Invoke-RestMethod http://localhost:18081/api/devices/online
```

10. 创建一个模拟任务，确认 Relay 下发的 `RemoteTask` 会携带 `authorizedDirectory`：

```powershell
$body = @{
  taskType = "LIST_DIR"
  payload = @{
    data = @{
      path = "src/main.java"
    }
  }
  timeoutSeconds = 30
} | ConvertTo-Json -Depth 5

Invoke-RestMethod -Method Post -Uri "http://localhost:8080/api/sessions/<sessionId>/tasks" -ContentType "application/json" -Body $body
```

11. 观察 Agent UI 日志，应看到任务信息和“任务授权目录”日志。

## 7. 启动/运行方式

构建：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

启动 Relay Server：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f relay-server\pom.xml spring-boot:run
```

启动 Agent Client：

```powershell
cd E:\openclaw-project\ai-remote-helper
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f agent-client\pom.xml javafx:run
```

运行后必须先在 Agent GUI 中选择授权目录，再连接服务器。

## 8. 遇到的问题与解决办法

问题 1：直接执行 `mvn clean package` 失败。

原因：当前 PowerShell PATH 没有 Maven。

解决办法：使用项目局部 Maven：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd clean package
```

问题 2：路径穿越必须被拒绝。

解决办法：`PathSandbox.resolveSecurely` 只接受相对路径，先用授权目录 resolve，再 normalize，最后检查结果是否仍以授权目录开头；`../../../etc/passwd` 会抛出 `SecurityException`。

问题 3：未选择授权目录不能连接。

解决办法：`AgentClientApplication` 在连接前检查 `authorizedDirectoryPath` 和 `pathSandbox`，缺失时直接在 UI 日志中提示并阻止连接。

问题 4：Relay 端不能接收缺失授权目录的 Agent 注册。

解决办法：`AgentConnectionController.hello` 校验 `authorizedDirectory`，为空时发送 `MISSING_AUTHORIZED_DIRECTORY` 错误并拒绝注册。

问题 5：本轮未做 GUI 端到端人工验证。

解决办法：本轮实际执行了单元测试和全量 Maven 构建；GUI 启动、在线设备查询和任务创建命令已在本文档中标注为“未执行，仅建议”。

## 9. 后续注意事项

- Phase 04 只完成授权目录与路径沙箱，不代表已经可以真实读取或写入文件。
- 后续所有真实文件读写必须调用 `PathSandbox.resolveSecurely`，不能直接拼接路径。
- 后续真实命令执行的 cwd 必须限制在授权目录内。
- `/api/devices/online` 返回的 `authorizedDirectory` 是 Agent 主动授权目录，Relay 只负责记录和转发，不得替用户扩大范围。
- Agent UI 必须始终可见，用户可一键断开。
- 不得实现隐藏运行、绕过授权、访问授权目录外文件、读取浏览器数据、读取 SSH 私钥、提权、修改注册表或规避审计日志。
